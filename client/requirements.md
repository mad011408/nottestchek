## Packages
react-markdown | For rendering assistant responses with rich text formatting
framer-motion | For smooth message animations and transitions
lucide-react | For beautiful icons (already in base, but listing for explicit usage)
clsx | For conditional class names (already in base)
tailwind-merge | For merging tailwind classes (already in base)

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
  mono: ["var(--font-mono)"],
}
